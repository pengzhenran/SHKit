SHKit v2.0.1 —— 任意散点 / 任意网格 ↔ 球谐系数。Windows 10/11（64 位），免费，完全离线运行。

**资产**：`SHKit_Setup_v2.0.1.exe`（约 92.1 MB）
**SHA256**：`84A82759FBD53CC6DCF547C3A130F777CDBFA43050C585C3851098770E009282`

### 本版修复

v2.0.0 之后的补丁版，版本号已同步到程序、Windows「应用和功能」与安装包名（三处必须一致，发布验收脚本守着这一点）。
本版同时把使用说明与界面截图按 v2.0 的九个页签重拍重写——v2.0 新增五个页签后，旧截图拍的是已经不存在的窗口。

### v2.0 功能

- **正变换（分析）**：任意散点 / 任意网格 → 球谐系数；**反变换（综合）**：系数 → 任意散点 / 任意网格
- **积分元自动定权**：7 种规则（`dh` / `glq` / `grid` / `voronoi` / `delaunay` / `uniform` / `user`），按采样几何自动选择
- **诊断**：求积完备性、条件数、Shannon 数、推荐阶数、逐条中文警告
- **稳健估计**：加权最小二乘、矩阵无关 CG、Tikhonov / Kaula 正则
- **球面 Slepian 局部化**：区域数据反演
- **批量多历元分析** `analyze-series`：一次解完整条序列，积分元与 Gram 只算一次，逐历元诊断表带 MAD 离群标记（只报告不剔除）
- **经度 FFT 快路径**：条件不满足自动回退并写明中文理由，**绝不静默换算法**
- **水平形变 u_N / u_E**：位系数的球面梯度（矢量场），极点用解析值；网格走 FFT，实测比直接法快 14–18×
- **时间域算子**：趋势 / 周年 / 半年拟合成系数序列，时间高斯平滑；缺测历元排除而不补 0
- **序列产品**：点序列 / 区域平均（报覆盖率）/ 场序列 `.nc`，落盘布局与 `3_grids/*.nc` 同构
- **界面九个页签**：地图、逐阶谱、诊断报告、系数统计、数值表、逐历元诊断、时间序列、趋势与周年、水平形变
- **交互增强**：统一色标面板、悬停双读数、可复制数值表、时间轴控件、播放与 GIF 导出、可暂停的逐时次导出
- **缓存与隔离**：分析结果按「源文件指纹 + 全部影响参数」缓存（拷到别的机器仍命中）；批量分析在 spawn 子进程里跑，子进程被强杀界面照常可用

实测（181×360 全球网格、`nmax=60`、203 历元）：批量分析直接法 5.07 s → **FFT 0.53 s**。

### 验证

19 套脚本共 **1161 项检查全绿**（含 GUI 离屏冒烟、单位换算、水平形变、多时间批处理、经度 FFT、时间域算子、序列产品与发布验收）。关键实测：Driscoll–Healy 全球网格恢复带限场 1.35e-15；GLQ 网格 5.09e-15；聚簇散点用 Voronoi 积分元把系数相对误差从 1.27e+00 降到 1.29e-01（9.8×）；迭代校正把往返精度从 7.4e-04 提到 3.0e-06（247×）。

### 安装

1. 下载本页的 `SHKit_Setup_v2.0.1.exe`，双击按向导安装。默认**按用户安装**（不弹管理员）到
   `%LOCALAPPDATA%\Programs\SHKit`；也可以在向导里改到 `Program Files`
2. 开始菜单生成 `SHKit` / `使用说明 (HTML)` / `卸载 SHKit`，可选桌面快捷方式
3. **不需要装 Python**：运行时、Qt、绘图库都在包内的 `_internal\` 里；随包还附带软件运行代码与测试（`source\`），便于核对与复现
4. 验证安装（退出码 0 即正常）：

```cmd
"%LOCALAPPDATA%\Programs\SHKit\SHKit.exe" --self-test
```

### 首次运行可能被 Windows 拦下

程序与安装包都没有代码签名：

- 「Windows 已保护你的电脑 / 未知发布者」→ 点「更多信息」→「仍要运行」
- 「智能应用控制已阻止可能不安全的应用」→ 这是 Windows 11 的智能应用控制，没有单应用白名单，只能关闭：设置 → 隐私和安全性 → Windows 安全中心 → 应用和浏览器控制 → 智能应用控制 → 关闭，然后重新双击程序（不必重装）

换安装目录、卸载重装都不能绕过第二种提示——它只看数字签名，不看路径。关闭智能应用控制不影响 Defender 的病毒防护。

### 使用注意

- **区域散点不能用 Voronoi 面积**：球面 Voronoi 剖分永远铺满整个球面，区域边界点的胞会膨胀到球的对侧（实测 30° 球冠虚高 14.9 倍）。区域散点请用 `delaunay`
- **`Δφ·Δλ·cosφ` 是近似**：精确球带面积是 `Δλ(sinφ₂−sinφ₁)`，前者系统性偏大 `(Δφ)²/24`。请用 `rule='grid'`
- **`S_n0 ≡ 0`**：正弦基只取 `m ≥ 1`，未知数个数是 `(L+1)²`
- 「输入是」与「输出为」是两个不同的声明，改「输入是」会改变导出的系数，改「输出为」只换输出场
- 区域数据在原理上不能唯一确定全球球谐系数，需先 remove–restore；覆盖率不足时程序会给出警告与推荐阶数

### 引用

1. Wang H., Xiang L., Jia L., Wu P., Steffen H., Wang Q., Chen L. (2012). Load Love numbers and Green's functions for elastic Earth models PREM, iasp91, ak135, and modified models with refined crustal structure from Crust 2.0. Computers & Geosciences, 49, 190–199. doi:10.1016/j.cageo.2012.06.022
2. Sun J. W., Wang L. S., Peng Z. R., Fu Z. Y., Chen C (2022). The sea level fingerprints of global terrestrial water storage changes detected by GRACE and GRACE-FO data. Pure and Applied Geophysics, 179(9), 3303–3317. doi:10.1007/s00024-022-03099-5
