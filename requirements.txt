# SHKit runtime dependencies
#
# Verified on: Anaconda Python 3.13.5, Windows 11
#
#   numpy   2.1.3
#   scipy   1.15.3      (SphericalVoronoi, ConvexHull, sparse CG, legendre.leggauss)
#
# Everything below is optional and only needed for specific features.

# --- required -------------------------------------------------------------
numpy>=1.24
scipy>=1.10

# --- optional -------------------------------------------------------------
pandas>=2.0          # reading/writing .csv / .xlsx points and tables
xarray>=2023.1       # reading/writing NetCDF grids
netCDF4>=1.6         # NetCDF backend for xarray
psutil>=5.9          # automatic memory-aware block sizing / CG fallback
matplotlib>=3.6      # only for the optional plotting helpers
